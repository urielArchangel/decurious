import React from 'react'

const AddSpeaker = () => {
  return (
    <section className='flex cursor-pointer items-center justify-center rounded-lg w-full border-2 border-dashed bprder-[var(--subtext)] h-[70px]'>
    <p className='text-[var(--subtext)]'>Add a speaker</p>
</section>  )
}

export default AddSpeaker